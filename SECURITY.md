# Security Policy

## 密钥与环境变量

核心流程不需要 API Key。未来可选集成只把密钥放入本地 `.env` 或系统凭据存储；不得写入源码、日志、Markdown、CSV 或 Git。`.env.example` 只列空变量名。

## 个人文件

原始文件只放在被忽略的 `candidate/source-documents/`、`private/`、`references/`、`certificates/`。处理时只读，不覆盖、不上传。CSV 不得存储证件号码、完整签证内容或不必要的敏感信息。

## 网站登录与自动化

禁止自动注册、登录、保存 Cookie/密码、绕过验证码/隐私授权/robots.txt/反自动化控制。登录墙或限制出现时停止，改为用户手动搜索并粘贴岗位正文。禁止自动上传、发送邮件和最终提交。

## 第三方脚本与依赖

执行前阅读来源和脚本内容；使用固定版本与项目本地虚拟环境；不运行来源不明的安装脚本。用 `python -m pip list`、包管理器审计及官方安全公告检查依赖。当前核心实现只用 Python 标准库。

## 风险报告

发现个人数据泄露、越权写入、自动提交路径、危险网络访问或依赖风险时，立即停止相关动作，在本地记录复现步骤、受影响文件和缓解措施，先告知用户再修复。

## 为什么禁止自动投递

最终申请会产生法律、隐私和声誉后果，也可能违反网站条款。人工提交是事实复核、授权、隐私同意和重复投递防护的最后控制点。
